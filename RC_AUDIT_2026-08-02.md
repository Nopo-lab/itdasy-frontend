# 릴리즈 후보 감사 (RC Audit) — 2026-08-02

> **수정 금지 모드.** 이 문서는 "무엇이 실제로 검증됐고 무엇은 아직인가" 만 적는다.
> 추측 금지 — 모든 항목에 확인 방법과 증거를 남긴다.

## 🔴 진행 중인 장애 (감사 중 발견)

**GCP 청구 계정이 닫혀 백엔드 전체가 12시간 넘게 중단.**

| 항목 | 값 |
|---|---|
| 근본 원인 | 청구 계정 `013F5A-96DF19-B93A67` 이 `open: false` |
| 노출 방식 | `gcloud run services update` → `reason: BILLING_DISABLED` |
| 마지막 정상 응답 | 2026-08-01T17:03:10Z (`/auth/me` 200) |
| 감사 시각 | 2026-08-02T05:28Z (**약 12.5시간 경과**) |
| 인스턴스 수 | 3시간 내내 active 0 / idle 0 (min-instances=1 인데도) |
| 로그 | 17:05 이후 **0건** — 컨테이너가 뜨지도 못함 |
| 프로젝트/API | 프로젝트 ACTIVE · Cloud Run API ENABLED · 쿼터 정상 (원인 아님) |
| 프론트 | GitHub Pages 200 (살아 있으나 모든 API 실패) |

⚠️ `billingInfo` 의 `billingEnabled=true` 는 **"청구 계정이 연결돼 있다"** 는 뜻일 뿐이다.
계정 자체가 닫히면 이 값은 true 인 채로 서비스가 죽는다 — 이것만 보고 정상이라 판단하면 안 된다.

**복구는 연준님만 가능** (청구 계정 소유자 권한 + 결제수단). 이게 풀리기 전까진 다른 어떤 검증도 진행 불가.

---

## ✅ 이번 장애로 **역으로 검증된** 것

| 항목 | 증거 |
|---|---|
| Discord 알림 파이프라인 | 실제 장애에서 발송 확인 — 워크플로 로그 `discord → HTTP 204` |
| 알림 감지 지연 | 장애 17:03 → 첫 실패 감지 **17:13** (약 10분) |
| Actions Uptime Alert 동작 | 17:13 이후 9회 연속 `failure` (= 계속 감지 중) |
| 킬스위치 격리 | 장애 전 실측: `FEATURE_CAPTION=off` → 캡션 503 · health 200 · 원복 후 200 |

---

## 지목 6항목 검증 결과

| # | 항목 | 결과 | 증거 |
|---|---|---|---|
| 1 | GCP Uptime Check 생성 | ✅ 존재 | `itdasy-backend-staging-health` · period 300s · timeout 10s · 6개 대륙 체커 |
| 1b | 〃 정상 동작 | ⚠️ **미확정** | 284회 전부 실패. **서비스가 실제로 죽어 있어 실패가 정상** — 단 통과하는 걸 본 적이 없어 설정 정합성은 복구 후 재확인 필요 |
| 2 | Alert Policy 연결 | ⚠️ 부분 | 정책 존재·enabled=true·필터에 check_id 포함 ✅ / **알림채널 0개 → 통보 안 됨** (콘솔에만 뜸) |
| 3 | Redis | ❌ **끊김** | 로그: `Error -3 connecting to redis.railway.internal:6379. Temporary failure in name resolution` → 메모리 폴백. maxScale=5 라 rate limit 이 인스턴스별로 쪼개짐 |
| 4 | used_nonces 마이그레이션 | ✅ 적용됨 | 로그: `Running upgrade 0032_money_checks -> 0033_used_nonces` @2026-08-01T16:18:30Z |
| 5 | CHECK 제약 운영 반영 | ✅ 적용됨 (간접) | 로그: `Running upgrade 0031 -> 0032_money_checks` @16:05:58Z. **정리 로그 `[0032]` 미출력 = 위반 행 0건**. DB 레벨 직접 질의는 서비스 다운으로 불가 |
| 6 | bootstrap 운영 동작 | ✅ 확인 | 로그: `[bootstrap] alembic_version 있음 — 기존 DB. 건드리지 않는다` (컨테이너 시작마다, 5회 확인) — 설계대로 기존 DB 무영향 |

### 마이그레이션 적용 이력 (라이브 로그 실측)

```
2026-08-01T16:18:30Z  0032_money_checks  -> 0033_used_nonces
2026-08-01T16:05:58Z  0031_payment_key_uq -> 0032_money_checks
2026-08-01T04:21:36Z  0030_revenue_txn_uq -> 0031_payment_key_uq
2026-08-01T03:45:53Z  0029_h4_token_epoch -> 0030_revenue_txn_uq
```

---

## 장애 **이전** 에 라이브 검증 완료된 항목 (17:03 이전, 유효)

| 항목 | 증거 |
|---|---|
| 매출 멱등 (동시 100발) | 재실측 → 1건 · 15.5s → 1.2s |
| 티켓 재생 차단 | 같은 티켓 2회차부터 401 · 동시 10발 → 200 ×1 / 401 ×9 |
| 티켓 jti 고유성 | 연속 발급 2개 서로 다름 · 각각 독립 200 |
| 금액 상한 | 매출 21억+1 → 422 |
| 수수료율 범위 | 3.4 → 422 |
| 우회 플래그 제거 | `ITDASY_STAGING_BYPASS_ALL` 제거 후 신규계정 plan=free 분리 확인 |
| 비용 상한 설정 | `ITDASY_DAILY_COST_CAP_KRW=50000` env 확인 |
| 킬스위치 | 캡션 503 / health 200 / 원복 200 |

---

## 검증 **못 한** 것 (사유 명시)

| 항목 | 사유 |
|---|---|
| CHECK 제약 DB 직접 질의 | 서비스 다운 — 운영 DB 직접 접근 경로 없음(Supabase 자격증명 미보유) |
| 회원권 충전 원자성 라이브 | 서비스 다운. 로컬 postgres·sqlite 스레드 테스트로는 확인됨 |
| Sentry 이벤트 수신 | Sentry API 토큰 미보유 + 백엔드 다운으로 신규 이벤트 발생 불가 |
| 프론트 스모크 재실행 | 백엔드 다운으로 의미 없음 |
| Uptime Check 통과 상태 | 서비스 복구 후에만 확인 가능 |
| IAP / Meta 콘솔 | 콘솔 접근 권한 없음 |
| 실기기 QA | 물리 기기 필요 |

_작성: 2026-08-02 · 감사 시작 시점 서비스 다운 상태_


---

## 🔬 장애 원인 진단 — 5항목 교차검증 (2026-08-02 06:2x)

> 증거 수준 표기: 🟢 Confirmed · 🟡 Strong evidence · 🟠 Suspected · 🔴 Unknown

### ① 리비전 conditions 전체 🟢

`itdasy-backend-staging-00389-frf` — **전부 True, 성공 메시지 포함**

| condition | 값 | 메시지 |
|---|---|---|
| Ready | True | Deploying revision succeeded in 24.33s |
| Active | True | — |
| **ContainerHealthy** | **True** | Containers became healthy in 16.84s |
| **ContainerReady** | **True** | Container image import completed in 4.7s |
| **MinInstancesProvisioned** | **True** | Min instances provisioned successfully in 19.04s |
| ResourcesAvailable | True | startup probe 통과 |

`desiredReplicas: 1` / **`actualReplicas: None`** ← 플랫폼은 1개를 원하는데 실제 0개

### ② 이벤트 로그 🟢
17:00 이후 severity>=WARNING **0건**. 실패 이벤트가 아예 없다.

### ③ 시작 로그 🟢 — 가장 결정적
```
2026-08-02T06:20:01Z  Starting new instance. Reason: MANUAL_OR_CUSTOMER_MIN_INSTANCE
(이후 아무 로그 없음)
```
플랫폼이 인스턴스 시작을 **시도했는데 앱 로그가 한 줄도 안 나왔다** —
`[bootstrap]`·alembic·uvicorn 전부 없음. **컨테이너가 실행 자체를 못 했다.**

### ④ 감사 로그 🟠
프로젝트 레벨엔 청구 이벤트 미기록(청구계정/조직 레벨 로그라 이 프로젝트에서 안 보임).
→ 청구가 **언제** 닫혔는지는 콘솔에서만 확인 가능.

### ⑤ 청구 복구 후 동일 리비전 생존 🔴 **미검증**
청구계정이 여전히 `open=false` 라 테스트 불가. **이게 마지막 확정 조건.**

---

## 원인 후보별 평가

⚠️ **표기 정정(2026-08-02)**: 처음엔 컨테이너 계열을 `❌ 배제 🟢` 로 적었으나 **과했다.**
리비전 conditions 는 **배포 시점(16:26) 의 상태**이고 서비스가 죽은 건 **17:03 이후**다.
"배포 때 healthy 였다" 가 "그 뒤에 죽지 않았다" 를 증명하지 않는다.
Cloud Run 에서는 *리비전 생성 정상 → min-instance 재생성 실패 → 런타임 종료* 처럼
시간축이 갈리는 경우가 실제로 있다. 따라서 배제가 아니라 **가능성 등급**으로 낮춘다.

| 후보 | 상태 | 근거와 그 한계 |
|---|---|---|
| Startup failure | 🟡 가능성 매우 낮음 | 16:26 배포 시 ContainerHealthy=True(16.84s). **단 이건 그 시점의 상태일 뿐** |
| Probe failure | 🟡 가능성 매우 낮음 | 같은 시점 ResourcesAvailable=True. 이후 프로브 실패 가능성은 배제 못 함 |
| Runtime crash | 🟡 가능성 매우 낮음 | 06:20 인스턴스 시작 시도 후 앱 로그 0건 → 코드가 안 돈 것으로 보임. **단 로그 전달 자체가 막혔을 가능성은 남음** |
| Image pull | 🟡 가능성 매우 낮음 | ContainerReady=True(import 4.7s). 이미지는 바뀐 적 없음 |
| Secret mount | 🟡 가능성 낮음 | 실패 시 보통 ContainerReady 가 False. 확정 배제는 아님 |
| VPC/네트워크 | 🟡 가능성 낮음 | ingress=all · IAM allUsers · RoutesReady=True |
| **Billing** | 🟡 **가장 유력** | `services update` → `reason: BILLING_DISABLED`(컨트롤 플레인 거절) · 계정 `open=false` |

### 최종 확정 실험 (🔴 미실시)

```
Billing OFF → 503                    (관측됨)
Billing ON  → 코드 변경 없이 정상     (미검증 ← 이게 남았다)
```

동일 리비전 `00389-frf` 가 **코드 한 줄 안 바꾸고** 살아나면 Billing 인과가 성립한다.
그 전까지는 어떤 후보도 🟢 로 배제하거나 확정하지 않는다.

---

## 종합 평가

| 항목 | 등급 |
|---|---|
| 코드 감사 | 🟢 신뢰 가능 (테스트 1,303) |
| DB 검증 (마이그레이션·제약) | 🟢 신뢰 가능 (실제 postgres 3경로) |
| 경쟁 조건 검증 | 🟢 신뢰 가능 (동시성 실측) |
| 운영 장애 존재 | 🟢 확정 (curl 2 URL + 브라우저 직접 이동) |
| Billing 이 원인 | 🟡 가장 유력 |
| 다른 원인 완전 배제 | 🔴 불가 — 복구 실험 전까지 |


---

## 📱 실기기 QA 2차 — 전 화면 뒤로가기 순회 (갤럭시 API 35)

화면마다 **앱을 재시작**해 격리 검증(상태 누적 오염 방지). CDP 로 DOM 을 직접 읽어 판정.

| 화면 | 오버레이 | 수정 전 | 수정 후 |
|---|---|---|---|
| 예약관리 | booking | ✅ 원래 정상 | — |
| 매출관리 | revenue | ✅ 원래 정상 | — |
| **잇비·자동화** | `#aiHubSheet` | ❌ 시트 그대로 · 뒤 탭만 홈으로 | 🟢 1회에 닫힘 |
| **고객관리** | `#customerSheet` | ❌ 무반응 | 🟢 3회 시행 전부 닫힘 |
| **연동관리** | `#integrationsHubSheet` | ❌ 무반응 | 🟢 닫힘(`[]`) |
| **설정** | `#settingsHubSheet` | ❌ 무반응 | 🟢 닫힘(`[]`) |

공통 원인: `_registerSheet` / `_markSheetOpen` / `_markSheetClosed` 미등록 →
`app-haptic.js` 백핸들러가 그 시트의 존재를 몰라 2단계('홈으로')·3단계('앱 종료')로 흘렀다.
커밋: 74382ea(자동화) · d239000(고객·연동·설정)

### 🟠 잇비 챗봇 첫 back 간헐 무시 — 미해결

5회 시행 중 **1회** 첫 back 이 무시됐다(4/5 성공). 등록은 정상(stack=assistant, registry=assistant).
관찰 전용 프로브로 확인한 실패 시점의 사실: **backButton 이벤트가 JS 에 도달조차 안 했다**(로그 `[]`).
성공 시점엔 `BACK수신 → POPSTATE → 닫힘` 이 정상 기록된다.
재현 조건을 특정하지 못했다. 테스트 하네스(CDP 클릭 직후 keyevent)의 타이밍 artifact 가능성도 남아 있어
**🟠 Suspected 로 두고 실기기에서 손으로 재확인 필요.**

### 🧪 이번 라운드에서 배운 검증 함정

1. **시트를 `display` 직접 조작으로 열면 안 된다** — opener 함수를 건너뛰어 "미등록" 오탐이 난다.
   실제 UI 경로(버튼 클릭)로만 열 것.
2. **화면 간 상태가 누적된다** — 앞 시트를 안 닫고 다음을 열면 오버레이가 4~5개 겹쳐
   판정이 전부 오염된다. 화면마다 앱 재시작이 안전하다.
3. **앱이 옛 `?v=` 를 로드하고 있을 수 있다** — Pages 원본엔 수정이 있는데 앱은 이전 커밋 버전을
   받고 있었다(`?v=…beba278` vs 라이브 `…d239000`). `pm clear` 로도 안 지워졌고
   SW·caches 도 0개였다 — **HTTP 캐시**였다. 검증 전 로드된 script src 의 `?v=` 를 반드시 확인할 것.
4. **에뮬레이터 앱 재시작 때마다 웹뷰 디버그 소켓 PID 가 바뀐다** — adb forward 를 갱신하지 않으면
   죽은 PID 를 가리켜 CDP 가 조용히 끊긴다.


---

## 🔴 P0 — 배포 캐시 전수 감사 (2026-08-02)

### 실측한 캐시 계층

| 계층 | 실측값 | 통제 가능? |
|---|---|---|
| GitHub Pages HTTP | `cache-control: max-age=600` · ETag 있음 | ❌ Pages 는 커스텀 헤더 불가 |
| Fastly CDN 엣지 | `via: 1.1 varnish` · `x-served-by: cache-icn…`(서울) · `x-cache: MISS/HIT` | ❌ |
| WebView HTTP 캐시 | 위 max-age 를 그대로 따름 | △ 앱 데이터 삭제로만 |
| Service Worker | HTML=network-first · JS/CSS=cache-first+SWR · `skipWaiting`+`clients.claim` · `updateViaCache:'none'` | ✅ |
| Cache Storage | SW 가 관리, 버전 키(`itdasy-<빌드>`) | ✅ |

### 원인 — 두 가지가 겹쳐 있었다

**(가) 배포 대기 로직이 틀렸다 🟢 — 이번 사고의 직접 원인**
`gh run list --limit 1` 로 판정하면 **직전 커밋의 이미 끝난 실행**을 보고 즉시 통과한다.
실측: `6e4fd48` 을 푸시하고 "배포 성공"을 확인했는데 그건 `96a4683` 의 실행이었고
내 커밋은 아직 `in_progress` 였다. **배포가 끝나기도 전에 QA 를 시작한 것.**
→ `scratchpad/waitdeploy.sh` 신설: 내 SHA 의 실행 완료를 기다린 뒤
   **라이브 index.html 에 그 SHA 가 실제로 보일 때까지** 한 번 더 확인한다.

**(나) 진입점이 낡으면 아무 가드도 안 걸린다 🟢 — 구조적 결함, 재현 확인**
기존 가드 2개는 **내부 정합성만** 본다(HTML↔JS, SW↔번들). 옛 index.html 과 그에 맞는
옛 JS 를 함께 받으면 셋이 서로 일치해서 **경고 없이 구버전이 돈다.**
실측: 앱이 `96a4683` 에 고정된 채 3 사이클 연속(라이브는 매번 갱신) 옛 버전을 로드했고,
그 시점 **SW 등록 0개 · Cache Storage 0개** 였다 → SW 가 아니라 **WebView HTTP 캐시**였다.

### 대응

`deploy.yml` 이 매 배포마다 `build.txt`(21바이트)에 빌드 버전을 쓰고, 앱은 부팅 3초 시점에
`cache:'no-store'` 로 받아 자기 `__LATEST_BUILD__` 와 대조 → 다르면 캐시·SW 비우고
**세션당 한 번만** 리로드. 실패 시 조용히 통과(오프라인에 앱이 죽으면 안 된다).

### 검증 — 배포/재시작 12 사이클

각 사이클: 실제 커밋 푸시 → 배포 완료 + 라이브 반영 확인 → 앱 force-stop → 재시작 →
로드된 `__LATEST_BUILD__` 와 **실제 script src 의 `?v=`** 를 라이브 `build.txt` 와 대조.

| 사이클 | 결과 | 비고 |
|---|---|---|
| 1~3 | ❌ | 앱이 `96a4683` 고정. **그 버전엔 가드가 아직 없다**(가드는 `6e4fd48` 에 들어감) |
| — | — | 앱 데이터 삭제로 교착 1회 해제 → 가드 포함 버전 로드 |
| **4~12 (9회)** | **✅ 전부 일치** | 라이브 = 최초로드 = 가드후 = script `?v=` |

증거는 매 사이클 실제 script URL 까지 기록(`…app-core.js?v=20260802-1127-c254ca3` 형태).

### 남은 한계 (정직하게)

- 가드는 **부팅 3초 시점에만** 돈다. 앱을 며칠 켜둔 채 배포가 나가면 다음 부팅까지 구버전이다.
  (작업 중 강제 리로드는 사용자 데이터를 날릴 수 있어 의도적으로 안 했다.)
- **가드가 없는 구버전에 이미 고정된 기기**는 스스로 못 빠져나온다. 한 번은 앱 데이터 삭제가 필요하다.
  출시 전이라 실사용자 영향은 없지만, 출시 후 같은 상황이 생기면 동일한 함정이 된다.
- CDN 엣지 전파 지연 자체는 여전히 존재한다. 가드는 그 뒤에 한 번 더 잡아줄 뿐이다.

### 🧪 앞으로의 QA 전제조건

1. 배포 후에는 **반드시 `waitdeploy.sh`** 로 라이브 반영까지 확인한 뒤 테스트 시작.
2. 테스트 전 **로드된 빌드 버전을 먼저 찍고** 시작한다(`window.__LATEST_BUILD__` + script `?v=`).
   라이브와 다르면 그 회차 결과는 **전부 무효**로 본다.

---

## 🧪 QA 규칙 추가 (P2)

- **실제 사용자 경로로만 테스트한다.** `display` 직접 변경·DOM 강제 조작 금지 —
  opener 함수를 건너뛰어 "미등록" 오탐이 난다(2026-08-02 실제로 냈다).
- **화면 이동마다 오버레이/백드롭/모달 개수와 history stack 을 찍는다.** 안 닫힌 시트가
  누적되면 이후 판정이 전부 오염된다(실측: 시트 5개가 동시에 떠 있었다).
- (환경 메모) 에뮬레이터는 앱 재시작마다 웹뷰 디버그 소켓 PID 가 바뀐다 →
  `adb forward` 를 갱신하지 않으면 죽은 PID 를 가리켜 CDP 가 조용히 끊긴다. 릴리즈 이슈는 아님.
