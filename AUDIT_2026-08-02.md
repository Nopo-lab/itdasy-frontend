# 도메인별 릴리즈 감사 — 2026-08-02

절차: `.ai/RELEASE_AUDIT_MATRIX.md` · 근거 규칙: `.ai/FEATURE_QA_PROMPT.md`
증거 수준: 🟢 독립 증거 2개 이상 / 🟡 반증 가능 / 🟠 단일 근거 / 🔴 근거 부족
증명 수준: **L4** 실제 재현 / **L3** 코드 흐름상 반드시 성립 / **L2** 외부 조건 확인 필요 / **L1** 후보
→ **P0 는 L3 이상만.** L2 이하는 `후보(L2)` 로 적고 P0 라 쓰지 않는다.
**수정은 하지 않았다. 조사만.**

기준 커밋: FE `beba278` · BE `73eb6a7`(감사 시작 시점. 진행 중 `07416c4` 로 이동 — 다른 세션이 같은 워킹트리에서 작업 중)

---

# 🧨 반증 라운드 결과 (2026-08-02, 3발견 × 3렌즈 = 9 독립 조사)

**규칙**: 반증에 실패한 것만 P0 유지. 외부 조건 확인이 필요하면 L2 로 내리고 '후보'로 적는다.
**모든 `refuted` 근거는 내가 직접 파일을 열어 재확인했다.**

| 발견 | 반증 전 | **반증 후** | 무엇이 바뀌었나 |
|---|---|---|---|
| **P0-1** 세션 고정 | P0 (L3) | **P0 (L3) 유지 · 범위 확대** | 입구가 1개가 아니라 **2개**였다. 새로 찾은 쪽이 **더 나쁘다**(검증 0) |
| **P0-2** 구독 갱신 미반영 | P0 (L3) | **후보 (L2) 하향** | 결함은 확정. 그러나 스토어 미출시 → **오늘 피해자 0.** 출시 시점에 자동 P0 승격 |
| **P0-3** 영수증 재사용 race | P0 (L3) | **후보 (L2) 하향** | **익스플로잇 서술이 틀렸다.** 단, 레이스가 필요 없는 **다른 우회 경로**를 새로 찾음 |

> 📌 **9건 중 반증 성공 0건.** 하지만 2건이 "결함은 맞는데 지금 터지지는 않는다"로 밝혀져 P0 에서 내려갔다. 이게 반증 절차의 성과다 — 이전 같으면 P0 3건으로 보고했을 것이다.

---

---

## 4. 예약 (🔴 Critical) — 완료

반증 6건(2발견 × 3렌즈) **전부 실패** → 둘 다 유지. 결정적 근거는 전부 내가 직접 재확인했다.

### 🔴 B-1 [P0 · 증명 L3] — **지운 예약이 시간칸을 영구 점유하고, 그 결과가 손님에게 잘못된 거절로 나간다**

**증거 수준: 🟢** (비대칭을 같은 레포 대조군 2개로 확증)

**사슬 (전부 파일:줄 확인)**

| # | 사실 | 근거 |
|---|---|---|
| 1 | `DELETE /bookings/{id}` 는 `deleted_at` 만 세팅하고 **`status` 는 `'confirmed'` 그대로** | [bookings.py:464](../itdasy_backend-test/backend/routers/bookings.py:464) |
| 2 | DM 확정 충돌쿼리 2곳이 `status.in_(['confirmed','completed'])` 만 보고 **`deleted_at` 을 안 본다** | [dm_confirm_queue.py:820](../itdasy_backend-test/backend/routers/dm_confirm_queue.py:820) · [:1103](../itdasy_backend-test/backend/routers/dm_confirm_queue.py:1103) |
| 3 | 그 파일에 `deleted_at` 이 **0회** 등장 (`calendar_slots.py` 도 **0회**) | `grep -c deleted_at` = 0 / 0 |
| 4 | 실패 문구가 `final_reply` 가 되어 **손님에게 실제 DM 발송** | [:574-576](../itdasy_backend-test/backend/routers/dm_confirm_queue.py:574) → [:596](../itdasy_backend-test/backend/routers/dm_confirm_queue.py:596) `_send_dm_tracked` |
| 5 | FE 는 실제로 DELETE 를 쓴다 (캘린더 시트 '삭제' 버튼) | [app-calendar-view.js:2237](app-calendar-view.js:2237) → [app-booking-api.js:213](app-booking-api.js:213) |
| 6 | 소프트삭제 행을 치우는 배치가 **없다** → 유령 슬롯 영구 | 레포 전체에 Booking purge 배치 0건 |

**결과**: 원장님이 예약을 지웠는데 캘린더는 비어 있고, 손님이 DM 으로 그 시간을 요청하면
**"이미 예약이 있어요 🥹"** 라고 거절당한다. 원장님은 왜 거절되는지 모르고, 손님은 떠난다. **조용한 매출 손실.**

**🔑 "관례다"가 성립하지 않는 이유 — 같은 레포 안 대조군 2개**
| 대조군 | 동작 |
|---|---|
| `bookings.py` (REST 충돌검사 `_has_conflict`) | `deleted_at.is_(None)` 을 **건다** (파일 내 `deleted_at` 10회) |
| `assistant.py:5581-5582` (잇비 취소) | `deleted_at` **과** `status='cancelled'` 를 **둘 다** 세팅 |

즉 코드베이스의 규칙은 "삭제분은 거른다"이고, **`dm_confirm_queue.py` 와 `calendar_slots.py` 두 파일만 빠졌다.**
마이그레이션 `0022` 가 UNIQUE 인덱스에 대해 정확히 같은 문제("취소된 예약이 슬롯 점유")를 고쳤는데 **쿼리 쪽은 안 고쳤다.**

**범위가 원 주장보다 넓다**: `calendar_slots.py` 도 `deleted_at` 0회라, 원장이 확정 카드를 보기 **전** 자동응답 단계부터 오염된다.

**반증 3렌즈 — 전부 실패**: ① 가드: 세션·모델·미들웨어·DB제약 어디에도 방어 없음 ② 경로: 파봤더니 `calendar_slots.py`·`assistant_undo.py` 로 **넓어짐** ③ 무효화: 7종 시도, `0022` 가 오히려 반대 증거.

**수정 비용**: 필터 한 줄 × 3곳.

---

### 🟠 B-2 [P1 · 증명 L3] — 겹치는 예약 이중부킹 (DB 제약이 `starts_at` 완전일치만 막는다)

**증거 수준: 🟢** · **P0 → P1 하향** (아래 이유)

- 유일한 DB 제약: `CREATE UNIQUE INDEX uq_booking_user_starts_active ON bookings (user_id, starts_at)` ([0022:41-45](../itdasy_backend-test/backend/alembic/versions/0022_booking_slot_excl_cancel.py:41)) — **컬럼이 `starts_at` 하나**라 10:00–11:00 vs 10:30–11:30 은 구조적으로 못 잡는다.
- 겹침은 `_has_conflict` ([bookings.py:62-74](../itdasy_backend-test/backend/routers/bookings.py:62)) 앱레벨 SELECT 뿐 — 락·제약 없는 read-then-write.
- **핵심 전제 실측 확인**: `create_booking` 은 **sync `def`** ([:126](../itdasy_backend-test/backend/routers/bookings.py:126)) → `fastapi==0.136.1` 의 `routing.py:330` 이 `run_in_threadpool` 로 넘긴다 → **1워커 uvicorn 에서도 진짜 병렬**. (IAP 의 async 경로와 정반대 — 거기선 이 논리가 방어였다)
- EXCLUDE/tsrange/btree_gist **0건**, `isolation_level` 미지정(READ COMMITTED), create 경로에 락 0.

**🚨 레이스가 필요 없는 경로 2개** (반증에서 새로 나옴): `smart_import.py:634` 과 `imports.py`(→`importer.py:293`) 는 **충돌검사 호출 자체가 없다** → 동시성 없이 **순차 단일 요청**으로 겹친 예약이 들어간다.

**🚨 PATCH 는 더 나쁘다**: `with_for_update()` 는 수정 대상 행만 잠그고 `_has_conflict` 는 여전히 무락. 게다가 create 에 있는 IntegrityError→409 변환이 **PATCH 에는 없어서**, 유일한 DB 가드가 걸리는 경우조차 **500** 이 된다.

**P1 로 내린 이유 3가지**
1. 가장 흔한 동시성(더블탭·재시도)은 `starts_at` 이 같아 **이미 UNIQUE 가 막는다**(2026-07-31 라운드에서 동시 10발 → 1건만 성공 실측).
2. FE 예약 생성 경로가 전부 순차(`Promise.all` 없음) → 자연 발생하려면 폰+PC·잇비·임포트 교차가 필요.
3. 겹친 예약 2건은 **캘린더에 그대로 보인다** — 매출 멱등키 같은 조용한 파손이 아니다.

> ⚠️ **한계**: 라이브 Postgres 동시 발사 재현은 **못 했다**(로컬 PG 없음·레포 수정 금지). 정확한 표현은 **"구조상 열려 있음"**이지 "실측으로 터졌음"이 아니다.

---

### ✅ 검증 완료 — 이상 없음

| 항목 | 근거 |
|---|---|
| 이중부킹(같은 시각) | partial UNIQUE + IntegrityError→409 변환 ([bookings.py:167-175](../itdasy_backend-test/backend/routers/bookings.py:167)) |
| 완료→매출 이중생성 | PATCH 가 `with_for_update()` 로 직렬화 ([:284](../itdasy_backend-test/backend/routers/bookings.py:284)) |
| 리마인더 중복발송 | advisory lock + 발송 마커 선커밋 ([reminder_scheduler.py:19,255](../itdasy_backend-test/backend/services/reminder_scheduler.py:19)) |
| 알림톡 무단발송 | 샵이 켠 경우에만(opt-in) `alimtalk_auto_enabled` |
| cross-tenant customer_id | create·PATCH 양쪽에 소유권 검증 |
| 예약 삭제 시 연결 매출 | 같이 삭제, 단 노쇼 위약금은 마커로 보존 |
| 손님 확인응답 멱등 | 같은 응답 재전송은 no-op |

### 📊 도메인 4 종료표

| 항목 | 개수 | 내역 |
|---|---|---|
| **P0 (L4)** | 0 | — |
| **P0 (L3)** | **1** | B-1 유령 슬롯 → 손님 오거절 |
| **P1** | **1** | B-2 겹침 이중부킹 |
| **P2** | 0 | — |
| **오탐** | 0 | 반증 6건 전부 실패 |
| **Dead Code** | 0 | — |
| **미구현** | 1 | 카카오 알림톡 템플릿(주석 명시, 원장 수동 복붙) |
| **TODO** | 0 | — |
| **권한 누락** | 0 | 전 엔드포인트 `Depends(get_current_user)` (손님 확인응답은 서명 토큰) |
| **API 미연결** | 0 | — |
| **프론트만 존재** | 0 | — |

### 🤔 도메인 4 — 내가 틀릴 수 있는 이유

1. **B-1 직격 · 빈도를 코드로는 못 정한다.** 예약 상세의 **'취소'**(PATCH `status='cancelled'`)를 쓰는 원장은 이 버그를 안 만난다. 실사용이 사실상 '취소'뿐이라면 B-1 은 P0 이 아니라 P2 다. 운영 DB 에서 `status='confirmed' AND deleted_at IS NOT NULL` 행 수를 세야 확정된다 — **조회하지 않았다.**
2. **B-2 직격 · 실측 재현을 안 했다.** 코드 구조(무락 read-then-write + 스레드풀 병렬 + READ COMMITTED)에서 **연역**했다. 로컬 Postgres 가 없어 동시 발사를 못 했다. `\d+ bookings` 를 실DB에 돌려야 EXCLUDE 제약 부재가 확정된다 — 코드 기준으로만 0건이다.
3. **"코드에 없다"를 "런타임에 없다"로 확대했다.** 누군가 콘솔에서 수동 DDL 을 했거나 `DATABASE_URL` 에 격리수준 파라미터가 주입돼 있으면 B-2 결론이 뒤집힌다. 이 세션은 **실DB를 한 번도 조회하지 않았다** — 메모리에 기록된 이전 오판(단일 도구·단일 시점 단정)과 같은 형태의 위험이다.
4. **세 렌즈가 독립 관측이 아니다.** 결론이 일치하지만 셋 다 **같은 정적 소스**를 읽었을 뿐이다. "세 번 확인했다"는 감각이 실제 증거 강도보다 확신을 부풀렸을 수 있다.
5. **`app-calendar-view.js:1999` 의 `Promise.allSettled`** 를 열어보지 않았다. 거기에 예약 생성이 섞여 있으면 B-2 의 "프론트는 전부 순차" 완화 논거가 약해진다.

---

## 1. 인증·권한·세션 (🔴 Critical) — 완료

### 커버리지

| 항목 | 방법 | 결과 |
|---|---|---|
| 전 엔드포인트 인증 | AST 파싱 356개 전수 | 무인증 34개 → 전부 정당 |
| IDOR | path param + 소유자필터 AST 스캔 | 후보 15개 → **전부 오탐** |
| 토큰 전송 | 외부 오리진 fetch 전수 | 유출 경로 없음 |

---

### 🔴 P0-1-b [증명 L3 · **반증 라운드에서 새로 발견 · 셋 중 가장 나쁨**] — 메인 화면 `?_t=` 가 **아무 검증 없이** 토큰을 저장한다

> ✅ **수정 완료 (2026-08-03, FE `6593678`)** — `?_t=` 분기 제거. 생성 측은 전수조사 0건이라 FE 단독 수정으로 끝났다.

**증거 수준: 🟢** (로컬 + **배포본 curl 로 같은 줄 확인**) · **L3**

**어디**: [app-core.js:2301-2311](app-core.js:2301) — 배포본 `app-core.js:2303` 에 **동일하게 존재**(curl HTTP 200, 171KB)

```js
const t = params.get('_t');
if (t) {
  const tok = decodeURIComponent(t);
  setToken(tok);                                    // ← 검증이 0 이다
  window.applyNewSession && window.applyNewSession(tok);
  history.replaceState(null, '', window.location.pathname);   // ← URL 에서 흔적까지 지운다
}
```

**P0-1 보다 나쁜 이유 3가지**
1. **`/auth/me` 검증조차 없다.** `oauth-return.html` 은 그래도 토큰이 유효한지는 봤다. 여기는 **아무것도 안 본다.**
2. **메인 진입 페이지다.** 별도 페이지가 아니라 `index.html` 이 로드하는 `app-core.js` 라, 공격 URL 이 `https://nopo-lab.github.io/itdasy-frontend-test-yeunjun/?_t=<공격자JWT>` 로 **앱 주소 그 자체**다.
3. **`history.replaceState` 로 URL 을 지운다.** 원장님이 주소창을 봐도 흔적이 없다.

**고아 코드다 (= 지워도 잃는 기능이 없다)**: `_t` 파라미터를 **생성하는 코드가 FE·BE 어디에도 없다.** 전수 grep 결과 `_t` 는 `app-gallery-write.js` 의 DOM id 접미사(`id + '_t'`)와 `output/` 안 QA 스크립트뿐. 주석의 "Chrome으로 이동 시 토큰 자동 복원"은 지금 존재하지 않는 흐름이다.

---

### 🔴 P0-1-a [증명 L3] — `oauth-return` 이 **공격자가 건네준 JWT 를 그대로 저장**한다 (세션 고정)

> ✅ **수정 완료 (2026-08-03, BE+FE 동시)** — 소비 측(FE `oauth-return.html`·`app-oauth-return.js`)과
> 생성 측(BE `google_oauth.py _build_return_url` — google/kakao/naver 공용)을 **같은 라운드에** 제거.
> challenge 없으면 `?error=pkce_required` 로 돌려보낸다. FE `_oauthPkceStart` 의 `catch → ''` 폴백도
> 막았다(스스로 레거시를 유발하던 설계). 회귀 테스트 신설: `backend/tests/test_oauth_return_url_pkce.py`
> (32케이스, 옛 코드에 되돌리면 16건 실패하는 것까지 확인).
> ⚠️ **배포 순서: FE 먼저 → SW 캐시 범프 확인 → BE.** 반대로 하면 구 FE 캐시가 `?error=` 를 못 읽는다.
> ⚠️ 남은 수동 확인: 구글·카카오·네이버 **3사 실제 로그인 1회씩**(웹 + 네이티브 딥링크).

**증거 수준: 🟢** (배포본 HTTP 200 실측 + 코드 무조건 분기 + 백엔드 발급 경로 교차확인)

**증명 수준: L3 (논리적으로 증명)** — P0 자격 충족

| 질문 | 답 |
|---|---|
| **왜 L3 인가** | 성립이 **외부 조건에 의존하지 않는다.** 유효한 JWT 하나가 주어지면 `sub`(토큰 payload)와 `me.id`(그 토큰으로 부른 `/auth/me` 응답)는 **같은 토큰에서 나오므로 항상 일치** → 유일한 게이트를 무조건 통과 → `store()` 실행. 이 흐름을 깨는 분기가 파일 안에 없다(전문 확인). 배포본에 분기가 실재함도 확인(HTTP 200 + 문자열 존재). |
| **왜 L4 가 아닌가** | 실제 공격자 JWT 를 발급해 E2E 재현하지 **않았다.** 재현하려면 계정 생성 또는 비밀번호 입력이 필요한데, **감사 규칙상 자격증명 입력·계정 생성은 하지 않는다.** 그래서 L3 에서 멈췄다. |
| **L4 로 올리는 절차** (연준님이 2분이면 가능) | ① 스테이징에 **테스트 계정 하나**로 로그인 → DevTools 콘솔에서 `localStorage.getItem('itdasy_token::staging')` 복사 ② **다른 브라우저(또는 시크릿창)** 에서 `…/oauth-return.html?token=<복사한값>&provider=google` 열기 ③ "✓ 로그인 완료!" 뜨고 그 창이 **테스트 계정으로 로그인돼 있으면 L4 확정.** (실제 공격도 이것과 완전히 동일한 절차다) |

**어디**
- 웹: [oauth-return.html:68-82](oauth-return.html:68) — `https://nopo-lab.github.io/itdasy-frontend-test-yeunjun/oauth-return.html` **HTTP 200 실측, 레거시 분기 배포본에 존재 확인**
- 네이티브: [app-oauth-return.js:95-118](app-oauth-return.js:95) — 딥링크 `itdasy://oauth-return?token=…`

**무엇이 잘못됐나**

두 파일 모두 `?token=<JWT>` 를 받으면 이렇게 검사하고 저장한다:

```js
var sub = JSON.parse(atob(token.split('.')[1])).sub;   // 토큰이 주장하는 주인
fetch(API + '/auth/me', { headers: { Authorization: 'Bearer ' + token } })
  .then(me => { if (String(me.id) !== sub) throw new Error('mismatch');
                store(token); });                       // ← 통과하면 저장
```

**이 검사는 공격자 자신의 정상 토큰을 절대 걸러내지 못한다.** `sub` 과 `me.id` 가 **같은 토큰에서 나오므로 정의상 항상 일치**한다. 걸러지는 건 위조·만료 토큰뿐이다.

주석은 이 검사가 세션 고정을 막는다고 적혀 있는데([app-oauth-return.js:95](app-oauth-return.js:95) "무검증 저장하면 세션 고정 공격이 되므로"), **그 주장이 성립하지 않는다.**

**공격 시나리오**
```
1. 공격자가 잇데이에 정상 가입 → 자기 JWT 확보
2. 원장님께 링크 전달 (카톡·문자·인스타 DM)
   https://nopo-lab.github.io/itdasy-frontend-test-yeunjun/oauth-return.html?token=<공격자JWT>&provider=google
3. 원장님이 클릭 → "✓ 로그인 완료!" → index.html 로 이동
4. 원장님은 로그인된 걸로 알고 그날 업무를 본다
   → 손님 이름·연락처·시술이력·매출·사진이 전부 공격자 계정에 쌓인다
5. 공격자가 자기 계정으로 로그인 → 전부 열람
```

네이티브가 더 나쁘다 — 같은 스킴을 등록한 악성 앱이 **사용자 클릭 없이** 딥링크를 쏠 수 있다.

**왜 아직 남아 있나 (2026-07-27 C-2 수정과의 관계)**

PKCE 수정(`e9ed875`·`c41afee`)은 **새 경로(`?code=`)만** 막았다. 레거시 `?token=` 분기는 "전환기 호환"으로 남겼고, 거기 붙인 `/auth/me` 검사가 방어라고 **잘못 판단**됐다.

정상 로그인은 안전하다 — FE 는 항상 PKCE 를 시도하고([app-core.js:2024](app-core.js:2024) `_oauthPkceStart`), 백엔드는 challenge 가 있으면 `code` 를 준다([google_oauth.py:150-164](../itdasy_backend-test/backend/routers/google_oauth.py:150)). **문제는 공격자가 백엔드를 거칠 필요가 없다는 것** — URL 은 직접 만든다.

**영향**: 원장님 영업정보 + 손님 개인정보(PIPA) 전량이 공격자 계정으로 유입. 심사(Apple·Meta) 리스크.
**발생 가능성**: 링크 클릭 1회 (네이티브는 클릭도 불필요) → 피싱 난이도 낮음.

**고치는 방향(참고, 미적용)**: 레거시 분기 삭제. 백엔드 폴백(`google_oauth.py:164`)도 같이 제거하면 `?token=` 전송이 앱 전체에서 사라진다. 현재 FE 는 항상 PKCE 를 보내므로 **정상 사용자 영향 0**. 구 프론트 캐시 걱정은 SW 캐시 범프가 이미 강제한다.

**적대검증(refute) 결과**
| 반박 시도 | 판정 |
|---|---|
| "백엔드가 `?token=` 을 안 주니 죽은 코드 아닌가" | ❌ 기각 — 공격자가 URL 을 직접 만든다. 게다가 `google_oauth.py:164` 폴백은 **살아 있다** |
| "`/auth/me` 검사가 막지 않나" | ❌ 기각 — `sub` 과 `me.id` 가 같은 토큰 출처라 항상 일치 |
| "페이지가 배포 안 됐을 수도" | ❌ 기각 — HTTP 200 + 배포본에 분기 존재 실측 |
| "원장님이 눈치채지 않을까" | ⚠️ 부분 인정 — 다른 계정이란 단서는 샵 이름뿐. 신규/미설정 계정이면 눈치채기 어려움 |

**🧨 반증 라운드(3렌즈) 결과 — 전부 실패 → L3 유지**
| 렌즈 | 시도 | 결과 |
|---|---|---|
| 다른 가드 | 저장 직전·직후·부팅·SW·CSP·백엔드에 막는 코드가 있는가 | ❌ **0건.** `setToken`([app-core.js:1011](app-core.js:1011))·부팅 검사([:753](app-core.js:753))는 **exp 만** 본다. 게이트도 토큰만 있으면 풀린다([:2342](app-core.js:2342)) |
| 다른 경로 | 입구가 이것뿐인가 | ❌ 반증 실패 — 오히려 **입구가 하나 더 있었다**(P0-1-b). `?token=` 분기는 PKCE verifier 검사([oauth-return.html:56-58](oauth-return.html:56))를 **아예 지나친다** |
| 무효화 근거 | 죽은코드·미배포·플래그OFF·CORS차단·테스트레포한정 5가설 | ❌ 5개 전부 실측으로 깨짐. 배포본과 로컬 파일이 **byte 동일**. CORS 는 `nopo-lab.github.io` 를 **허용**한다(curl 실측) |

**추가 확인**: 저장 시 `itdasy_token::staging` 과 `::prod` **양쪽에** 쓴다([oauth-return.html:42](oauth-return.html:42)) → 백엔드 설정과 무관하게 고정된다.

> ⚠️ **한계 명시**: 실제 공격자 JWT 를 발급해 E2E 재현하지는 **않았다**(SECRET_KEY 필요, 운영 데이터 오염 우려). 판정 근거는 **배포본 코드의 무조건 분기 + 검사 로직의 논리적 무효성**이다.

---

### 🟡 P2-1 [증명 L3] — CSP 에 `script-src` 가 없다

**증거 수준: 🟢** ([index.html:10](index.html:10)) · **L3** — 헤더 문자열 자체가 증거라 코드 흐름만으로 확정. (실제 XSS 를 태워보지 않아 L4 아님)

```html
<meta http-equiv="Content-Security-Policy" content="object-src 'none'; base-uri 'self'">
```

`script-src` 가 없어 **어떤 오리진의 스크립트든 실행 가능**하다. XSS 가 하나라도 뚫리면 제약이 없다.

이건 `SECURITY_AUDIT_2026-07-27.md` C-1(“앱 전체 코드를 GitHub Pages 에서 원격 로딩”)의 잔여분이고, 당시 **가드레일만 적용하고 수용된 위험**으로 기록돼 있다. 새 발견이 아니라 **출시 게이트에서 한 번 더 확인할 항목**으로 올린다.
외부 CDN 3종은 이미 SRI(`integrity`) 가 붙어 있다([index.html:123,130,2390](index.html:123)).

---

### 🟠 P1-1 [증명 L3] — 소셜 로그인 원장님에게 **절대 성공 못 하는 '비밀번호 변경' 버튼**이 보인다

**증거 수준: 🟢** · **증명 L3**

**흐름 (전부 무조건 성립)**
1. 소셜 가입자는 비밀번호를 **모른다** — 백엔드가 `secrets.token_urlsafe(32)` 를 해시해 넣는다 ([google_oauth.py:101-104](../itdasy_backend-test/backend/routers/google_oauth.py:101), 카카오·네이버 동일 헬퍼)
2. 그런데 설정에 **'비밀번호 변경' 버튼이 항상 뜬다** — [index.html:1135](index.html:1135). `user_oauth_provider` 값은 localStorage 에 이미 있는데([app-core.js:931](app-core.js:931)) **숨김 로직이 어디에도 없다**(전수 grep 0건)
3. 누르면 '현재 비밀번호'를 요구하고 `verify_password` 로 대조 ([auth.py:544](../itdasy_backend-test/backend/routers/auth.py:544)) → **무조건 실패**
4. 원장님이 보는 문구: **"현재 비밀번호가 올바르지 않습니다"** — 자기가 뭘 잘못했는지 알 수 없다

**왜 L3**: 세 지점(랜덤 비번 생성 · 숨김 로직 부재 · current 대조)이 모두 무조건 경로다. **왜 L4 아님**: 소셜 계정으로 실제 로그인해 눌러보지 않았다(자격증명 필요).

**영향**: 기능 실패는 아니지만 이 레포가 이미 한 번 정리한 유형(`21539d0` "유도 후 안 되는 CTA 제거/숨김")의 재발. 계정 탈취 의심 상황에서 원장님의 첫 대응이 막힌다.
**참고**: 완전히 막힌 건 아니다 — `/auth/forgot-password` 이메일 재설정으로는 비번을 만들 수 있다. 다만 그 안내가 이 화면에 없다.

---

### ✅ 검증 완료 — 이상 없음 (재감사 불필요)

| 항목 | 근거 | 증거 |
|---|---|---|
| **무인증 엔드포인트 34개 전부 정당** | webhook(HMAC 서명검증) · OAuth 콜백 · login/register · 공개설정(`/plans`·`/config`·`/categories`) | 🟢 AST 356개 전수 + 개별 확인 |
| **IDOR 없음** | 후보 15건 전부 헬퍼로 소유권 검사 — `_resolve_log`(+`FOR UPDATE`) · `_owned_service` · `_collect_draft_context` · `_get_or_create` | 🟢 헬퍼 본문까지 확인 |
| **PKCE 정상** | S256, TTL 120초, HMAC stateless | 🟢 `utils/security.py:66-101` |
| **1회용 코드가 진짜 1회용** | `used_nonces` PK 충돌로 재생 차단 (동시 재생도 하나만) | 🟢 `utils/nonce_store.py` |
| **로그아웃/비번변경/재설정이 전 세션 무효화** | `users.min_valid_iat` 토큰 에폭 | 🟢 `auth.py:377,554,610` |
| **세션검사 fail-closed(503)** | 401 아닌 503 선택 — 401이면 FE 가 토큰을 지워 DB 딸꾹질이 전원 강제 로그아웃이 됨 | 🟢 `utils/security.py:158-180` |
| **로그인 무차별 대입 방어** | IP당 제한 + **Redis 죽었을 때 DB 폴백**(login/register/forgot). 메모리만이면 Cloud Run maxScale=5 라 실효 5배 느슨해짐을 인지하고 대응 | 🟢 `utils/rate_limiter.py:79-131` |
| **비밀번호 정책** | 8자+영문+숫자+흔한비번차단+4연속반복차단 | 🟢 `auth.py:61-74` |
| **토큰이 외부로 안 나감** | 401 재첨부 오리진 게이트 + 외부 host fetch 전수 확인(0건) | 🟢 `app-core.js:1255` + rg 전수 |
| **바이오메트릭 평문 저장 제거됨** | H-2 수정 확인 | 🟢 `app-biometric.js:41-47` |
| **open-redirect 차단** | `return_to` 화이트리스트(`itdasy://`·`nopo-lab.github.io`) | 🟢 `instagram.py:132-141` |
| **PII 로그 마스킹** | `mask_email`/`hash_email` | 🟢 `auth.py:306` |
| **비밀번호 변경 배선 정상** | `changePwBtn`(index.html:1135) → `openChangePwModal`(app-core.js:2993) → `POST /auth/change-password`. 뒤로가기 등록·에폭 무효화까지 | 🟢 (단 소셜 계정 노출은 P1-1) |
| **인증 도메인 TODO/스텁 0건** | auth·3종 oauth·security·rate_limiter·nonce_store·password_reset 전수 | 🟢 rg 0건 |

### 📊 도메인 1 종료표

| 항목 | 개수 | 내역 |
|---|---|---|
| **P0 (L4)** | 0 | — |
| **P0 (L3)** | **2** | P0-1-b `?_t=` 무검증 토큰 저장(**메인 화면·고아 코드**) · P0-1-a OAuth `?token=` 레거시 분기 |
| **P1** | **1** | P1-1 소셜 계정에 성공 불가능한 '비밀번호 변경' 버튼 노출 |
| **P2** | 1 | P2-1 CSP `script-src` 부재(기존 C-1 잔여, 수용된 위험) |
| **오탐** | **15** | IDOR 후보 15건 — 전부 헬퍼로 소유권 검사 중 |
| **Dead Code** | 0 | — |
| **미구현** | 0 | — |
| **TODO** | 0 | 인증 도메인 파일 전수 rg 0건 |
| **권한 누락** | **0** | 엔드포인트 356개 AST 전수 → 무인증 34개 전부 정당 |
| **API 미연결** | 0 | — |
| **프론트만 존재** | 0 | (P1-1 은 API 가 있고 응답도 정상. '쓸 수 없는' 것이지 미연결 아님) |

### 🤔 도메인 1 — 내가 틀릴 수 있는 이유

1. **P0-1 직격**: 부팅 경로에서 토큰을 **재검증하는 코드를 못 봤을 수 있다.** 나는 `oauth-return` 두 파일과 `_handle401` 만 읽었지, `applyNewSession`·`_bootAuth`·서비스워커가 저장된 토큰을 어떻게 취급하는지는 **전수로 안 봤다.** 만약 부팅 때 "이 토큰이 이 기기에서 로그인한 것인지" 확인하는 장치가 어딘가 있다면 P0-1 은 성립하지 않는다. → **반증 렌즈 1 로 확인 중**
2. **AST 스캔이 데코레이터를 놓쳤을 수 있다.** 인증 커버리지 356개는 `@router.<verb>` 패턴만 셌다. `APIRouter(dependencies=[...])` 로 **라우터 레벨에 인증이 걸린 경우**는 "무인증"으로 잘못 셌을 수 있고(=과소평가는 안전), 반대로 커스텀 데코레이터로 감싼 엔드포인트는 **아예 못 셌을 수 있다**(=진짜 누락을 놓침).
3. **IDOR '오탐 15건' 판정은 헬퍼 본문 4개만 읽고 내렸다.** `_resolve_log`·`_owned_service`·`_collect_draft_context`·`_get_or_create` 는 확인했지만, 15건 전부의 호출 인자가 **정말 `Depends(get_current_user)` 의 값인지**는 한 건씩 대조하지 않았다. 어딘가 body 에서 온 `user_id` 를 넘기고 있다면 IDOR 이 살아 있다.
4. **런타임을 안 봤다.** 운영 DB 에 `min_valid_iat` 가 실제로 채워지는지, 레이트리밋 DB 폴백이 실제로 도는지는 코드로만 판단했다. `used_nonces` 테이블이 운영에 없으면 nonce·레이트리밋 방어가 조용히 통과된다(`consume` 이 예외 시 `True` 반환).

---

## 2. 결제·구독·IAP (🔴 Critical) — 완료

### 🟠 후보-2 [증명 **L2** · 반증 라운드에서 P0 → 하향] — **돈 내는 원장님이 첫 갱신일에 무료로 강등된다** (구독 갱신 미반영)

> **왜 P0 가 아닌가**: 결함(갱신이 서버에 절대 반영되지 않음)은 **확정**이다. 반증 3렌즈 모두 실패했다.
> 그러나 **피해가 성립하려면 실제 IAP 결제자가 있어야 하는데, 앱이 아직 스토어에 없다**(`STORE_SUBMISSION.md` — Play Console 승인 대기).
> 규칙상 "외부 조건 확인 필요" = **L2 = P0 자격 상실**. 단 **출시하는 순간 자동으로 P0 로 승격되는 확정 블로커**다.
>
> **반증 3렌즈 요약** — ① 가드: `/iap/status`·`/billing/renew-due` 본문까지 읽었으나 전부 무해 ② 경로: `next_bill_at` 쓰기 지점 **3곳 전수** + **APScheduler 잡 7개 전수**(구독 잡 0개) 확인, 사슬 못 끊음 ③ 무효화: "credentials 미주입이라 도달 불가"(README 근거)를 **gcloud 실측으로 스스로 깨뜨림**.
> **덤으로 나온 것**: `app-plan.js:256` 의 "플러그인 없음(현재 빌드)" 주석이 **stale** 이다 — `cordova_plugins.js` 에 `cordova-plugin-purchase 13.18.0`, `capacitor.build.gradle:22` 에 `billing:9.0.0` 이 실재한다.

**증거 수준: 🟢** · **증명 L3** — 7개 고리 전부 코드로 확인, 끊는 분기 없음

**연쇄 (하나라도 끊기면 성립 안 하는데, 전부 확인됨)**

| # | 고리 | 근거 |
|---|---|---|
| 1 | 구매 시 `next_bill_at` = 영수증 만료일로 세팅 | [iap.py:258](../itdasy_backend-test/backend/routers/iap.py:258) |
| 2 | Apple 갱신 알림(`DID_RENEW`)이 와도 **로그만 찍고 끝** | [iap.py:449-453](../itdasy_backend-test/backend/routers/iap.py:449) `# TODO (Phase 2): notificationType 분기` |
| 3 | Google 갱신 알림(`RENEWED`)도 **로그만** | [iap.py:549-552](../itdasy_backend-test/backend/routers/iap.py:549) 동일 TODO |
| 4 | 앱은 **부팅 때 IAP 스토어를 초기화하지 않는다** — `_ensureInit()` 호출처가 `purchaseMembership()`·`restore()` **딱 2곳** | [app-iap.js:161,201](app-iap.js:161) (rg 전수) |
| 5 | → 자동갱신 트랜잭션을 받는 `store.when().approved()` 리스너가 **등록조차 안 됨** | [app-iap.js:136](app-iap.js:136) — `_ensureInit` 안에 있음 |
| 6 | 그래서 `next_bill_at` 은 **최초 구매 이후 영원히 안 늘어난다** | rg 전수: 갱신 지점은 `_upsert_subscription`(IAP)·`billing.py`(PortOne) 뿐 |
| 7 | 만료일이 지나면 **`plan="free"`, `status="expired"` 로 강등** | [subscription.py:120-124](../itdasy_backend-test/backend/routers/subscription.py:120) |
| 8 | 이 강등은 **앱 켤 때마다 실행** (부팅 1.5초 뒤 `/subscription/status`) | [app-plan.js:374](app-plan.js:374) |

**결과**: Apple·Google 은 **계속 6,900원을 청구**하는데, 앱은 그 원장님을 **무료 사용자로 취급**한다. 캡션 3회/일·누끼 2회/일로 막힌다.

**🚨 이 버그는 출시 전 테스트로 절대 안 잡힌다** — **첫 갱신일(구매 +1개월)에야** 나타난다. 스모크·QA·심사 통과 후 한 달 뒤 유료 고객 전원에게 동시에 터진다.

**⚠️ 자체 반증에서 나온 정정 — 증상은 두 갈래이고, 어느 쪽인지 확정 못 했다**

강등은 `next_bill_at` 이 **NULL 이 아닐 때만** 일어난다 ([subscription.py:120](../itdasy_backend-test/backend/routers/subscription.py:120) 의 `sub.next_bill_at and …`).
그런데 `expires_at` 은 영수증에 만료일이 없으면 `None` 이 된다 ([iap.py:100](../itdasy_backend-test/backend/routers/iap.py:100), Google 도 동일 [:191](../itdasy_backend-test/backend/routers/iap.py:191)).

| | 조건 | 증상 |
|---|---|---|
| **갈래 A** | `expires_date_ms` 있음 (자동갱신 구독의 정상 응답) | 첫 만료일에 **유료 고객이 free 로 강등** |
| **갈래 B** | 없어서 `next_bill_at = NULL` | **해지·환불해도 영원히 유료** (강등 조건 자체가 안 걸림) |

자동갱신 구독(`PAID_SUBSCRIPTION`)이면 Apple 이 `expires_date_ms` 를 주므로 **갈래 A 가 유력**하지만, 실제 영수증을 못 봐서 확정 못 한다.
**어느 갈래든 결함이고 뿌리는 같다**(갱신·만료가 서버에 반영되지 않음). 다만 "첫 갱신일에 강등된다"는 서술은 갈래 A 조건부다.

**환경 실측 (gcloud, 스테이징)**: `APPLE_IAP_SHARED_SECRET` **있음** / `GOOGLE_PLAY_SERVICE_ACCOUNT_JSON` **없음**
→ 지금 스테이징에서 살아 있는 건 **Apple 경로뿐**. Google 은 409 "준비 중"으로 막힌다.

**복구 경로는 있다(단, 원장님이 알아야 함)**: 플랜 화면의 **'구매 복원'** 버튼 → `restore()` → `_ensureInit()` → `restorePurchases()` → `approved` → 재검증 → `next_bill_at` 연장. 네이티브에서만 보인다([app-plan.js:191](app-plan.js:191)).

**왜 L4 아님**: 실제 갱신을 기다리거나 Sandbox 구독(Apple Sandbox 는 1개월=5분)을 태워보지 않았다. 실기기 + 스토어 계정이 필요하다.
**L4 로 올리는 절차**: Apple Sandbox 계정으로 구독 → 5분 대기(Sandbox 1개월) → 앱 재실행 → 플랜 화면이 '무료'로 표시되면 확정.

---

### 🟠 후보-3 [증명 **L2** · 반증 라운드에서 P0 → 하향] — 영수증 재사용 차단이 **동시 요청에 뚫린다** (1결제 → N멤버십)

> **🔴 내 익스플로잇 서술이 틀렸다 — 정정한다.**
> "계정 2개가 동시에 치면 둘 다 통과한다"고 썼는데, **현재 배포 형태에서는 성립하지 않는다.**
> 직접 확인한 것:
> - `_upsert_subscription` 은 `async` 가 아니라 **동기 함수이고 본문에 `await` 이 0개**다 (AST 로 확인).
> - `backend/Dockerfile:41` = `uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}` — **`--workers` 없음** = 인스턴스당 이벤트루프 1개.
> - → **같은 인스턴스에 떨어진 두 요청은 SELECT→commit 사이에 끼어들 수 없다.**
>
> 경합은 `--max-instances 5` 로 **스케일아웃된 뒤에만** 열린다(동시 60 초과 유발 필요). 여기에 "진짜 결제된 영수증"과 스토어 출시라는 외부 조건이 더 얹힌다. → **L2.**
>
> **구조적 결함 자체는 확정이다**(제약을 만들 수 있는 4경로 — 모델 `__table_args__` 없음 · alembic 0001~0033 에 `subscription` **0건** · `main.py` 부트스트랩 ALTER 는 plain 컬럼 · `db_bootstrap` 의 `create_all` — 전부 확인). 수정은 **마이그레이션 1개 + `except IntegrityError`** 로 값싸다.

### 🟠 후보-3b [증명 L3(코드) / L2(도달)] — **레이스 없이** 영수증 바인딩이 지워진다

반증 '다른 경로' 렌즈가 찾았고 **내가 직접 확인했다.**

- [iap.py:257](../itdasy_backend-test/backend/routers/iap.py:257) 이 **무조건 덮어쓴다**: `sub.purchase_token = purchase_token`
- 그런데 [apple-verify(:279-288)](../itdasy_backend-test/backend/routers/iap.py:279) 는 **`purchase_token` 을 아예 안 넘긴다** → 기본값 `""`([:217](../itdasy_backend-test/backend/routers/iap.py:217))

→ 구글로 구독한 계정이 애플 검증을 **한 번** 하면 `purchase_token` 이 `""` 로 **지워진다.**
그 구글 토큰은 이제 **아무 계정에나 다시 묶일 수 있다.** 동시 요청이 전혀 필요 없는 **결정론적** 경로다.
반대 방향도 같다 — 애플로 묶인 계정이 구글 검증을 하면 `original_transaction_id` 가 덮인다([:256](../itdasy_backend-test/backend/routers/iap.py:256)).

지금은 구글 검증이 409(서비스계정 미주입)라 막혀 있어 **L2**. 보안 문제이기 이전에 **플랫폼 전환 시 구독 바인딩이 소실되는 데이터 결함**이다.

**증거 수준: 🟢** · **증명 L3**

**어디**: [iap.py:220-241](../itdasy_backend-test/backend/routers/iap.py:220) `_upsert_subscription`

```python
_clash = db.query(Subscription).filter(Subscription.user_id != user_id) \
           .filter(Subscription.original_transaction_id == _binding_id).first()
if _clash: raise HTTPException(409, ...)     # ← SELECT
...
db.commit()                                  # ← 그 다음 INSERT/UPDATE
```

**전형적인 read-then-write.** 계정 A·B 가 같은 영수증으로 **동시에** `/iap/apple-verify` 를 치면, 둘 다 SELECT 시점에 상대 커밋을 못 봐서 **둘 다 통과**한다(Postgres 기본 READ COMMITTED 는 이 팬텀을 막지 않는다).

**DB 제약이 없다** — 확인:
- `Subscription.original_transaction_id` = `index=True` 뿐, **`unique` 없음** ([models.py](../itdasy_backend-test/backend/models.py))
- `Subscription.purchase_token` = 인덱스조차 없음
- 마이그레이션 `0031_payment_key_uq` 는 **`payment_history.payment_key`**(PortOne 웹결제) 대상이라 **IAP 와 무관**

코드 주석도 이미 인정하고 있다 — *"(DB unique 인덱스는 후속 마이그레이션 권장 — 여기선 앱레벨 방어.)"* [iap.py:224](../itdasy_backend-test/backend/routers/iap.py:224)

**이 레포가 이미 배운 교훈의 재발**: 매출 멱등키가 **동시 100발에 6건** 생성돼 `0030` UNIQUE 로 막았고, 결제(`0031`)까지 훑었는데 **IAP 만 빠졌다.**

**공격**: 6,900원 1번 결제 → 계정 N개에서 동시 verify → 전원 멤버십.
**환경 확인 완료**: `APPLE_IAP_SHARED_SECRET` 이 **스테이징 Cloud Run 에 설정돼 있다**(gcloud 실측) → 검증 경로가 살아 있어 도달 가능. (그래서 L2 가 아니라 L3)

**왜 L4 아님**: 실제 Apple 영수증이 필요하고, 재현하려면 스토어 계정으로 결제해야 한다.

**자체 반증 2건 — 둘 다 실패(= 발견 유지)**
| 반박 시도 | 확인 | 결과 |
|---|---|---|
| "DB 격리수준이 SERIALIZABLE 이면 팬텀이 막힌다" | `database.py` 의 `create_engine` 에 **`isolation_level` 미지정** → Postgres 기본 READ COMMITTED | ❌ 반증 실패 |
| "`Subscription` 에 `__table_args__` 로 unique 가 걸려 있을 수 있다" | Subscription 클래스에 **`__table_args__` 자체가 없다**(AST 덤프 확인). `models.py:429` 의 것은 **`PersonaFeedback`** 것이었다. alembic 전체에도 `subscriptions` unique 인덱스 **0건** | ❌ 반증 실패 |

---

### 🟠 P1-2 [증명 L3] — 환불해도 한 달간 유료 기능을 계속 쓴다

`REFUND`(Apple) · `REVOKED`(Google) 알림이 **로그만 남는다**([iap.py:451](../itdasy_backend-test/backend/routers/iap.py:451), [:551](../itdasy_backend-test/backend/routers/iap.py:551)). 환불받아도 `next_bill_at` 이 그대로라 만료일까지 유료 권한이 유지된다. P0-2 와 같은 뿌리(TODO Phase 2)지만 방향이 반대라 별건으로 센다.

---

### ✅ 검증 완료 — 이상 없음

| 항목 | 근거 | 증거 |
|---|---|---|
| **플랜 한도 단일 소스** | `utils/plan_limits.PLAN_LIMITS` 하나. `subscription.PLANS`(표시값)가 **이걸 읽어간다** → 표시 3회 / 실제 100회 두 벌 문제 해소됨 | 🟢 `plan_limits.py:64` |
| **`membership` 이 유료로 인식** | `is_paid_or_bypass` · `_current_plan` 둘 다 포함 | 🟢 `plan_limits.py:219` |
| **만료 시 실제로 강등** | `_current_plan` 이 `status not in (active,trial)` → free | 🟢 `plan_limits.py:196` |
| **결제 웹훅 서명검증 + 재조회** | 본문만 믿지 않고 PortOne REST 재조회 후 반영 | 🟢 `billing.py:294-320` |
| **결제 멱등** | `payment_key` UNIQUE(`0031`) — 웹훅·verify 동시 도착 방어 | 🟢 마이그레이션 확인 |
| **`/billing` 재시도 금지** | FE 전역 fetch `NO_RETRY_PATH_RE` — 이중청구 차단 | 🟢 `app-core.js:1135` |
| **구매 실패 시 `finish()` 안 함** | 검증 실패면 스토어가 다음 기회에 재통지(과금 후 미활성 자가복구) | 🟢 `app-iap.js:135` |
| **구매 중복 탭 방어** | `_pending` 가드 + 120초 안전망 | 🟢 `app-iap.js:162,171` |
| **웹에서 IAP 무회귀** | 플러그인 없으면 `isAvailable()=false` → 기존 동작 | 🟢 `app-iap.js:160` |
| **Pub/Sub OIDC 검증** | `google-rtdn` audience 검증 | 🟢 `iap.py:460-` |

### 📊 도메인 2 종료표

| 항목 | 개수 | 내역 |
|---|---|---|
| **P0 (L4)** | 0 | — |
| **P0 (L3)** | **0** | 반증 라운드에서 2건 모두 L2 로 하향 |
| **후보 (L2)** | **3** | 후보-2 갱신 미반영 · 후보-3 영수증 race · 후보-3b 바인딩 덮어쓰기. **셋 다 결함은 확정, 발현 조건이 미충족(스토어 미출시·Google 미주입)** |
| **P1** | 1 | P1-2 환불 후에도 만료일까지 유료 유지 |
| **P2** | 0 | — |
| **오탐** | 1 | "만료 강제 안 됨" 의심 → 부팅마다 `/subscription/status` 가 지연 강등 실행(정상) |
| **내 서술 오류** | **1** | 후보-3 의 "동시 2발이면 뚫린다" — uvicorn 워커 1개라 같은 인스턴스에선 창이 안 열린다 |
| **Dead Code** | 0 | — |
| **미구현** | **2** | `apple-notification`·`google-rtdn` 의 notificationType 분기(수신·로그만) |
| **TODO** | **3** | `iap.py:363`(x5c 체인 검증) · `:449` · `:549` |
| **권한 누락** | 0 | verify 2종 인증 필수, 알림 2종은 서명/OIDC 검증 |
| **API 미연결** | 0 | — |
| **프론트만 존재** | 0 | — |

> ⚠️ **`billing.py:355 renew-due` 는 "골격만"** 이지만 PortOne 웹결제는 **전략상 제외**(CLAUDE.md: 토스/웹결제 미적용)라 미구현으로 세지 않았다.

### 🤔 도메인 2 — 내가 틀릴 수 있는 이유

1. **P0-2 직격 · `next_bill_at` 이 NULL 이면 강등 자체가 안 일어난다.** [subscription.py:120](../itdasy_backend-test/backend/routers/subscription.py:120) 조건이 `sub.next_bill_at and ... < now` 라, 만료일이 비어 있으면 **영원히 유료로 남는다.** Apple 영수증에 `expires_date_ms` 가 없는 경우([iap.py:100](../itdasy_backend-test/backend/routers/iap.py:100) `expires_at = None`) 실제로 NULL 이 된다. 그렇다면 P0-2 는 "강등된다"가 아니라 **정반대로 "영원히 안 끊긴다"**가 되고, 내 서술은 틀린다. **운영 DB 의 실제 값을 안 봤다.**
2. **P0-2 직격 · Apple S2S 알림이 애초에 안 올 수도 있다.** App Store Connect 에 알림 URL 이 등록돼 있는지 확인 못 했다. 등록 안 돼 있으면 "분기 미구현"은 **지금은 무해**하고(어차피 안 옴), 등록돼 있으면 내 진단대로 손실이 발생한다. 어느 쪽인지에 따라 P0 여부가 갈린다.
3. **P0-3 직격 · DB 격리수준을 확인 안 했다.** "READ COMMITTED 라 팬텀이 통과한다"고 썼는데, `database.py` 의 엔진 설정에 `isolation_level` 이 지정돼 있거나 미들웨어에 락이 있으면 성립하지 않는다. **`database.py` 를 안 열어봤다.**
4. **IAP 는 코드만 봤고 한 번도 실행 안 했다.** cordova-plugin-purchase 의 실제 동작(`restorePurchases` 가 갱신 트랜잭션을 `approved` 로 올리는지, 부팅 없이도 pending 트랜잭션이 전달되는지)은 **플러그인 문서·구현에 의존**하는데 확인하지 않았다. 내 사슬 4~5번 고리가 여기서 깨질 수 있다.
5. **`Subscription.user_id` 가 unique 라는 점의 함의를 끝까지 안 따졌다.** P0-3 에서 계정 2개는 행 2개라 충돌이 없다고 판단했는데, upsert 로직이 예상과 다르게 동작할 여지를 실행으로 확인하지 않았다.

---
