# 도메인별 릴리즈 감사 매트릭스

> **쓰는 법: 맨 아래 §8 프롬프트를 복사 → 도메인 이름만 바꿔서 한 번에 하나씩 돌린다.**
> 위 §0~§7 은 그 프롬프트가 참조하는 재료다. 감사는 **조사만** 하고 수정하지 않는다(수정은 승인 후 별도 라운드).

## 이 문서가 겹치지 않는 것 (먼저 확인)

| 문서 | 역할 | 이 문서와의 관계 |
|---|---|---|
| `.ai/APP_FEATURE_INDEX.md` | **무엇이 있나** (파일 매핑 SSOT) | 감사 대상 파일은 **전부 여기서** 찾는다. 여기 중복 안 씀 |
| `.ai/FEATURE_QA_PROMPT.md` | **어떻게 검증하나** (12축 · 환경세팅 · 헤드리스 함정 · 증거 수준 규칙 · 보고 형식) | 실행 절차는 **전부 그쪽**. 이 문서는 안 베낌 |
| 루트 `RELEASE_AUDIT_2026-07-31.md` 등 3종 | **그때 뭐가 나왔나** (시점 결과물) | 결과 기록. 이 문서는 **틀**이라 시점이 없다 |
| **이 문서** | **무엇을 · 어느 순서로 · 어디까지 파나** (도메인 단위 감사 틀) | — |

> 🔬 **증거 수준 표시(🟢🟡🟠🔴)와 "시점 다른 증거로 단정 금지" 규칙은 `FEATURE_QA_PROMPT.md` §결론 규칙이 정본.** 이 문서로 돌리는 감사에도 그대로 적용된다.

---

## §0. 감사 단위 — 도메인 12개

`APP_FEATURE_INDEX.md` 도메인 맵(21행)을 **감사 단위 12개**로 묶었다. 파일 매핑은 인덱스를 본다.

| # | 도메인 | 등급 | 핵심 질문 |
|---|---|---|---|
| 1 | **인증·권한·세션** | 🔴 Critical | 남의 계정으로 들어가지나? |
| 2 | **결제·구독·IAP** | 🔴 Critical | 돈을 두 번 받나? 안 받고 열어주나? |
| 3 | **회원권** | 🔴 Critical | 잔액이 맞나? |
| 4 | **예약** | 🔴 Critical | 이중부킹 되나? 확정이 실제로 도나? |
| 5 | **매출** | 🟠 High | 숫자가 맞나? 지워도 남나? |
| 6 | **고객 CRM** | 🟠 High | 남의 손님이 보이나? 개인정보 새나? |
| 7 | **DM·통합 인박스** | 🟠 High | 손님한테 잘못 나가나? 두 번 나가나? |
| 8 | **댓글 문의 응대** | 🟠 High | 공개 답글이 잘못 달리나? |
| 9 | **잇비(AI 비서)** | 🟠 High | 말로 시킨 게 돈·예약을 진짜로 바꾸나? |
| 10 | **작업실**(업로드→레이아웃→편집→캡션→발행) | 🟡 Medium | 만든 게 사라지나? |
| 11 | **연동**(인스타·네이버·카카오) | 🟡 Medium | 끊겼는데 연결됐다고 하나? |
| 12 | **홈·설정·알림·백업** | 🟢 Low | 화면이 예쁘게 나오나? |

---

## §1. 기능 등급 (Critical / High / Medium / Low) — *무엇을 얼마나 깊게 파나*

등급은 **감사 깊이**를 정한다. 결함 심각도(§2)와 축이 다르다.

### 🔴 Critical — 틀리면 **돈·계정·법**이 걸린다
> 인증·권한 · 결제·구독·IAP · 회원권 · 예약확정

- **전 항목 필수.** §4 체크리스트 19개를 **하나도 스킵 안 함**.
- **동시성은 반드시 `Promise.all` 동시 100발**로 때린다. 순차 통과는 증거가 아니다.
- **DB 제약(UNIQUE·CHECK)까지 확인.** 애플리케이션 코드 방어는 방어선으로 안 친다.
- 실패 시 **출시 중단**.

### 🟠 High — 틀리면 **손님이 떠나거나 잘못된 메시지가 나간다**
> DM · 댓글응대 · 매출 · 고객 CRM · 잇비

- §4 체크리스트 중 **보안 4종(XSS·권한·IDOR·주입)과 Race** 필수.
- **발송류는 "두 번 나가나"를 최우선**으로 본다(재시도·멱등·큐 중복).
- 실패 시 출시 전 수정.

### 🟡 Medium — 틀리면 **원장님이 만든 게 없어진다**
> 작업실 · 연동 · 게시물

- **데이터 안전(저장·복원·기기간 동기화)과 되돌리기** 중심.
- 보안은 XSS만(입력이 LLM·화면을 거치므로).
- 실패 시 백로그 가능(단 '작업 소실'은 High 로 승격).

### 🟢 Low — 틀리면 **불편하다**
> UI·애니메이션·아이콘·카피·빈 화면

- 12축 중 UX동선·카피·접근성만.
- `TECH_DEBT.md` 한 줄로 충분.

> ⚠️ **승격 규칙 3개** — Low 라도 이러면 즉시 등급을 올린다:
> ① 원장님이 만든 데이터가 **사라진다** → High
> ② 안 되는데 **성공이라고 말한다**(거짓 토스트) → High (실제 사고: `app-save-file.js` 이전 상태)
> ③ 화면에 **손님 실명·연락처**가 새 나간다 → Critical

---

## §2. 결함 트리아지 (P0 / P1 / P2) — *찾은 걸 언제 고치나*

§1 등급은 **기능**에, P 등급은 **발견된 결함**에 붙인다. 둘은 대체로 같이 가지만 **항상 같지는 않다** — Critical 도메인에서 오타를 찾으면 P2 다.

정본 매트릭스는 `RELEASE_AUDIT_2026-07-31.md` §0.5(영향 × 발생가능성). 도메인 기본값만 여기 둔다:

| | 내용 | 도메인 |
|---|---|---|
| **P0** | 돈이 새거나 · 계정이 뚫리거나 · 심사가 막힌다 | 결제 · 회원권 · 예약 · 로그인 · 권한 |
| **P1** | 손님에게 잘못 나가거나 · 숫자가 틀린다 | DM · 댓글 · 매출 · 고객 · 잇비 |
| **P2** | 원장님이 불편하다 | 게시물 · 작업실 · 설정 · 연동 |

**적대검증(refute)을 통과한 발견만 매트릭스에 올린다.** 실측 근거: 2026-07-27 전수사냥에서 한 에이전트가 돈 관련 7건을 플래그했는데 **진짜는 1건**이었다.

---

## §3. 페르소나 × 시나리오 — *누가 · 무엇을 하다가 깨지나*

### 3-1. 공통 페르소나 8 (모든 도메인에 적용)

| 페르소나 | 상태 | 이 사람에서만 터지는 것 |
|---|---|---|
| **신규 원장** | 가입 직후, 데이터 0 | 빈 화면·0으로 나누기·"3건 있어야" 안내 누락 |
| **기존 원장** | 데이터 수백 건 | 페이지네이션·느린 쿼리·목록 잘림 |
| **멤버십(유료)** | `plan = membership` | 한도 해제가 실제로 되나 |
| **무료 사용자** | 한도 걸림 | **표시 한도 ≠ 실제 한도** (실제 사고: 표시 3회 / 실제 100회) |
| **만료 사용자** | 구독 끝남 | 기능이 잠기나? 기존 데이터는 보이나? |
| **인터넷 끊김** | 오프라인·불안정 | 저장 거짓 성공 · **HTTP 503 을 오프라인으로 오판**(실제 사고) |
| **동시접속** | 폰 + PC 두 기기 | 마지막 저장이 이기나? 동기화 desync |
| **관리자** | admin 권한 | 남의 데이터 접근 · 로그에 개인정보 |

### 3-2. 도메인별 시나리오 (페르소나와 곱한다)

**예약**
```
예약 생성 · 예약 수정 · 예약 취소 · 예약 중복(같은 손님)
동시간 예약(이중부킹) · 회원권으로 예약 · 예약 완료(→매출 전환)
예약 노쇼 · 예약 변경(날짜만/시간만) · 과거 날짜 · 자정 넘김 · 월 경계
```

**매출**
```
매출 등록 · 매출 수정 · 환불(음수) · 금액 0원 · 금액 21억 초과
동시 저장(같은 멱등키) · 삭제 · 월 변경 · 카드 수수료율 음수
예약 완료 → 매출 자동생성 · 그 예약을 취소했을 때
```

**회원권**
```
판매 · 충전 · 차감(사용) · 잔액 부족 · 만료 · 만료 후 차감 시도
해지·환불 정산 · 재충전 시 만료일 · 계정 병합 시 잔액 이관
동시 충전 10발 · 동시 차감 10발
```

**결제·IAP**
```
첫 구매 · 구매 복원 · 같은 영수증 재사용 · 다른 계정에서 같은 영수증
구독 갱신 · 구독 만료 · 결제 실패 · 결제 중 앱 종료 · 동시 결제 요청
웹(플러그인 없음)에서 결제 버튼
```

**DM**
```
새 문의 수신 · AI 초안 생성 · 원장 승인 발송 · 원장 수정 후 발송
운영시간 외(자리비움) · 같은 손님 반복 문의(12h dedup)
발송 중 네트워크 끊김(→재시도로 2번 나가나) · 채널별(인스타/톡톡)
심사 미통과 상태에서 발송 시도
```

**고객 CRM**
```
수동 추가 · 예약에서 자동 등록 · 중복 손님 · 병합 · 삭제
메모(의료=민감정보 30일 만료) · 남의 계정 손님 조회(IDOR)
전화번호 형식 · 개인정보 내보내기 · 탈퇴 시 전부 지워지나
```

**작업실**
```
사진 1장 · 2장(전후) · 11장(초과) · HEIC · 대용량
레이아웃 적용 후 편집 · 편집 후 재오픈 · 다른 기기에서 이어서
캡션 생성 · 발행 · 예약 발행 · 발행 중 앱 종료 · 초안 저장
```

**잇비**
```
조회형 질문 · 실행형 명령(돈·예약 변경) · 애매한 명령 · 사진 첨부
"그 예약" 같은 지시대명사 · 상대 날짜(3일 뒤) · 실행 확인 후 이중 실행
LLM 타임아웃 · 쿼터 초과(429)
```

---

## §4. 표준 체크리스트 19 — *무엇을 확인하나*

**이 앱 기준으로 이미 판정해 뒀다.** "해당없음"은 스킵이 아니라 **한 번 확인하고 끝낸 것**이니, 근거가 바뀌면 다시 켠다.

| # | 항목 | 이 앱에서 | 확인 방법 |
|---|---|---|---|
| 1 | 생성 | ✅ | 정상 + 빈값 + 최대값 |
| 2 | 수정 | ✅ | 부분 수정 시 다른 필드 안 날아가나 |
| 3 | 삭제 | ✅ | 연결된 것(매출·예약)까지 지워지나 / 안 지워지나 |
| 4 | 권한 | ✅ 필수 | BE `Depends(get_current_user)` 유무 |
| 5 | 백엔드 존재 | ✅ 필수 | §5 파이프라인 |
| 6 | API 존재 | ✅ 필수 | `@router.` 데코레이터 실재 |
| 7 | DB 저장 | ✅ 필수 | `commit()` + 모델 컬럼 |
| 8 | Validation | ✅ 필수 | Pydantic 스키마 + **금액 상·하한** |
| 9 | **Race condition** | 🔴 **최우선** | `Promise.all` 동시 100발 → **DB UNIQUE/CHECK 있나** |
| 10 | XSS | ✅ 필수 | 사용자·**LLM 출력**이 `innerHTML` 로 가나 (`_esc` 사용 여부) |
| 11 | CSRF | ⚪ **해당없음** | 세션 쿠키가 아니라 **`Authorization: Bearer` 헤더**(`app-core.js:1070`). 단 **OAuth state·PKCE 는 확인** |
| 12 | SQL Injection | 🟡 **일부만** | SQLAlchemy ORM 기본이라 대부분 무해. **진짜 표면 = `nl_query.py`(NL→SQL) + raw `text()`/`execute()` 쓰는 라우터**(admin·assistant·customers·imports·retention 등) |
| 13 | IDOR | 🔴 **추가 필수** | 남의 `id` 로 GET/PATCH/DELETE. **실제 사고: 예약 PATCH `customer_id` IDOR** |
| 14 | 모바일 | ✅ | Safe-area · 노치 · 입력 자동확대 · **PC 사이드바 잘림** |
| 15 | 오프라인 | ✅ | **HTTP 에러(503)를 오프라인 취급 안 하나** · 저장 거짓 성공 |
| 16 | 복구 | ✅ | 재시도 · 자가복구 · 되돌리기(↩) |
| 17 | 로그 | ✅ | 개인정보(실명·이메일·JWT) 평문 로그 금지 |
| 18 | Sentry | ✅ | `index.html:2389`(app-core 앞) — 이 경로 에러가 실제로 잡히나 |
| 19 | Toast·에러메시지 | ✅ | `showToast()`(`app-core.js:329`). **거짓 성공 금지** · 401/422 문구 구분 |

> ➕ **이 앱 전용 3개 (19개에 없지만 반복해서 터진 것)**
> 20. **뒤로가기 등록** — 풀스크린 오버레이가 `_markSheetOpen` 안 하면 **안드로이드 back 에 앱이 그냥 꺼진다**(미등록 7화면 발견 이력)
> 21. **캐시 `?v=`** — 새로 만든 파일에 `?v=` 가 붙었나 (배포 자동 범프는 **이미 붙은 것만** 갱신)
> 22. **한도 두 벌** — 화면에 쓴 숫자와 서버 강제 숫자가 같나

---

## §5. 구현 여부 조사 파이프라인 — *진짜 되는 기능인가*

**이 앱에서 가장 많이 나온 결함 유형이 "있는 것처럼 보이는데 안 되는 기능"이다.** 순서대로 내려가다 끊기는 지점이 답이다.

```
프론트 버튼 있음
  ↓ 클릭 핸들러가 붙어 있나?          → 없으면: 장식용 버튼
백엔드 API 있음?
  ↓ @router 데코레이터 실재하나?      → 없으면: 404
라우팅 연결됨?
  ↓ main.py include_router 됐나?      → 없으면: 404
권한 있나?
  ↓ Depends(get_current_user) 있나?   → 없으면: 무인증 노출
DB 저장됨?
  ↓ commit() + 모델 컬럼 있나?        → 없으면: 새로고침하면 사라짐
실사용 가능?
  ↓ 플래그·env·심사로 잠겨 있나?      → 잠겼으면: 정직한 안내가 있나
Dead Code?  ← 호출하는 곳이 0개인가
Stub?       ← 함수는 있는데 pass / return None
TODO?       ← 주석만 있고 구현 없음
404 / NotImplemented?
```

### 확인 명령 (그대로 복사)

```bash
# 1) 프론트 버튼 → 핸들러
rg -n 'data-act="예약저장"' --type js
rg -n "on\('예약저장'|addEventListener" --type js

# 2) 백엔드 API 실재
rg -n '@router\.(get|post|patch|put|delete)' ../itdasy_backend-test/backend/routers/bookings.py

# 3) 라우팅 연결
rg -n 'include_router' ../itdasy_backend-test/backend/main.py | rg bookings

# 4) 권한
rg -n 'Depends\(get_current_user\)' ../itdasy_backend-test/backend/routers/bookings.py | wc -l

# 5) 죽은 코드 — 정의는 있는데 부르는 데가 없다
rg -n 'function _openFoo|_openFoo\s*=' --type js      # 정의
rg -n '_openFoo\(' --type js | wc -l                   # 호출 (1이면 정의뿐 = 죽음)

# 6) 스텁·TODO·미구현
rg -n 'TODO|FIXME|NotImplemented|pass\s*$|return None\s*$' \
   ../itdasy_backend-test/backend/routers/bookings.py
```

### ⚠️ 이 파이프라인이 실제로 잡은 것 (전부 라이브였다)

| 발견 | 끊긴 지점 |
|---|---|
| `auto_confirm` **예약 자동확정 토글** | 프론트 토글은 있는데 **백엔드가 아무것도 안 함** |
| `/persona/posts/ingest/instagram` | API 는 있는데 **아무도 호출 안 함** → 말투 학습이 시작조차 안 됨 |
| `getMyLayouts()` '내 레이아웃' | **항상 빈 목록** — 데이터를 만드는 코드가 main 에 없음 |
| 킬러 위젯 | 로드는 되는데 **붙을 컨테이너가 없어** 안 뜸 |
| `_markOffline` | **정의된 적 없는데 두 곳에서 호출** |
| 잇비 자동화 토글 | 실행 배관은 26 kind 지원인데 **enum 이 ~10개만 허용**해 나머지 잠김 |

> 🔑 **판정은 정규식 말고 실물로.** 스키마는 실DB, CSS 는 CSSOM, 죽은 코드는 AST·실행. `rg` 만으로 단정했다가 오판한 이력이 여러 건이다(`rg -r` 이 replace 로 먹은 사고 포함).

---

## §6. 테스트 시나리오 10 케이스 — *어떻게 때리나*

**모든 도메인 공통 10 케이스.** 도메인 시나리오(§3-2)를 이 10개에 곱한다.

| # | 케이스 | 기대 |
|---|---|---|
| 1 | 정상 | 성공 + 화면 반영 + DB 반영 |
| 2 | 빈값 | 막고 **왜 막혔는지** 말함 |
| 3 | 중복 | 두 번째는 안 만들어짐(멱등) |
| 4 | **동시 요청 100발** | **정확히 1건.** `Promise.all` 로 발사 |
| 5 | 권한 없음 | 401/403. **남의 id 로도 막힘(IDOR)** |
| 6 | 네트워크 끊김 | 거짓 성공 없음 · 재시도 · 자가복구 |
| 7 | 새로고침 | 저장된 게 살아 있음 |
| 8 | 뒤로가기 | **앱이 안 꺼짐** · 이 화면만 닫힘 |
| 9 | 만료 토큰 | 재로그인 유도(문구가 401/422 구분) |
| 10 | 서버 500/503 | **오프라인으로 오판 안 함** · 저장 잠금 안 걸림 |

**Race 발사 스니펫** (브라우저 콘솔):
```js
const body = { /* 같은 payload, 같은 멱등키 */ };
const rs = await Promise.all(Array.from({length:100}, () =>
  window.apiFetch('/revenue', {method:'POST', headers:{'Content-Type':'application/json'},
                               body: JSON.stringify(body)}).then(r=>r.status)));
console.log(rs.filter(s=>s<300).length, '건 생성됨');   // 1 이어야 한다
```

---

## §7. 실사용 하루 흐름 — *이어 붙였을 때도 되나*

한 기능씩 통과해도 **이어 붙이면 깨진다.** 페르소나별로 끝까지 태운다.

### A. 신규 미용실 원장 (첫날) — 🔴 Critical 경로
```
가입 → 샵 이름 입력 → 온보딩 → 인스타 연동(또는 건너뛰기)
  → 고객 추가 → 예약 생성 → 예약 확정
  → 회원권 판매 → 시술 완료 → 매출 등록(자동 전환)
  → 사진 업로드 → 레이아웃 → AI 캡션 → 발행
  → 로그아웃 → 재로그인(데이터 그대로인가)
```
**여기서 봐야 할 것:** 빈 데이터 화면 · 0으로 나누기 · "3건 있어야" 안내 · 온보딩 이탈 후 복귀.

### B. 기존 원장 (평일 하루) — 🟠 High 경로
```
아침 브리핑 확인 → 오늘 예약 확인 → DM 문의 답장(승인 발송)
  → 댓글 문의 공개 답글 → 손님 방문·시술 완료 → 매출 등록
  → 회원권 차감 → 노쇼 처리 → 저녁 게시물 작성·발행
  → 성과 확인
```
**여기서 봐야 할 것:** 발송 중복 · 숫자 정합(예약↔매출↔회원권) · 홈 카운트가 안 얼어붙나.

### C. 사고 상황 — 🔴 Critical 경로
```
지하철(신호 불안정)에서 예약 저장 → 실패 → 재시도
  → 서버 503 한 번 → (오프라인 배너 뜨나? 저장 잠기나?)
  → 회복 후 자동으로 풀리나 → 중복 저장 안 됐나
  → 폰과 PC 동시 접속 → 같은 예약 수정 → 어느 쪽이 이기나
  → 구독 만료 → 기능 잠김 → 기존 데이터는 보이나 → 결제 → 즉시 풀리나
```

---

## §8. 붙여넣는 도메인 감사 프롬프트

> 도메인 이름만 바꿔 **하나씩** 돌린다. 한 번에 두 도메인 금지(근거가 섞인다).

```
`.ai/APP_FEATURE_INDEX.md` 를 기준으로 '<도메인명>' 도메인만 릴리즈 감사 모드로 분석하라.

[범위] 프론트 · 백엔드 · DB · API · 권한 · 보안 · 동시성 · 실사용 시나리오 전부.

[절차] `.ai/RELEASE_AUDIT_MATRIX.md` 를 따른다.
 1. §0 에서 이 도메인의 등급을 확인하고, §1 이 정한 깊이만큼만 판다.
 2. §5 구현 여부 파이프라인을 **화면 요소 하나하나에** 적용한다.
    (프론트 버튼 → API → 라우팅 → 권한 → DB → 실사용 → dead/stub/TODO/404)
 3. §4 체크리스트 19+3 을 적용한다. '해당없음'은 근거를 적는다.
 4. §3 페르소나 8 × 이 도메인 시나리오를 곱해 빠진 경우를 찾는다.
 5. §6 10 케이스로 때린다. **동시성은 Promise.all 100발** — 순차 통과는 증거로 안 친다.

[찾을 것] 미구현 기능 · 죽은 버튼 · 연결 안 된 API · 보안 취약점(XSS·IDOR·권한) ·
         Race Condition · 예외처리 누락 · 거짓 성공 · 한도 두 벌 · 뒤로가기 미등록.

[근거 규칙] `.ai/FEATURE_QA_PROMPT.md` 의 결론 규칙을 적용한다.
 - 모든 결론에 증거 수준(🟢🟡🟠🔴)을 붙인다.
 - 시점이 다른 증거로 다른 시점을 단정하지 않는다.
 - 판정은 정규식 말고 실물로(스키마=실DB, CSS=CSSOM, 죽은코드=호출처 실측).
 - 발견마다 적대검증(refute)을 한 번 거친다. 과잉 플래그 이력이 있다.

[출력] 체크리스트 형태. 발견은 심각도(P0/P1/P2) 순. 각 항목에 파일:줄 근거.
       검증 못 한 것은 '미검증'으로 사유와 함께 분리해 적는다.

[금지] 🚫 수정하지 마라. 커밋하지 마라. 조사만 한다.
```

### 권장 순서 (돈 → 사람 → 물건)

```
1. 인증·권한   2. 결제·IAP   3. 회원권   4. 예약        ← 🔴 여기서 막히면 출시 중단
5. 매출        6. 고객 CRM   7. DM       8. 댓글  9. 잇비  ← 🟠
10. 작업실     11. 연동      12. 홈·설정                    ← 🟡🟢
```

### 진행 트래커

| # | 도메인 | 감사일 | P0 | P1 | P2 | 상태 |
|---|---|---|---|---|---|---|
| 1 | 인증·권한·세션 | | | | | ⬜ |
| 2 | 결제·구독·IAP | | | | | ⬜ |
| 3 | 회원권 | | | | | ⬜ |
| 4 | 예약 | | | | | ⬜ |
| 5 | 매출 | | | | | ⬜ |
| 6 | 고객 CRM | | | | | ⬜ |
| 7 | DM·통합 인박스 | | | | | ⬜ |
| 8 | 댓글 문의 응대 | | | | | ⬜ |
| 9 | 잇비 | | | | | ⬜ |
| 10 | 작업실 | | | | | ⬜ |
| 11 | 연동 | | | | | ⬜ |
| 12 | 홈·설정·알림·백업 | | | | | ⬜ |

> 감사 **결과**는 여기 쓰지 말고 `AUDIT_<도메인>_<날짜>.md` 로 따로 남긴다. 이 문서는 **틀**이라 시점이 없어야 한다.

---

_이 문서 = 감사 틀. "무엇이 있나"는 `APP_FEATURE_INDEX.md`, "어떻게 검증하나"는 `FEATURE_QA_PROMPT.md`._
_작성: 2026-08-02 (출시감사 라운드에서 반복해 나온 결함 유형을 틀로 굳힌 것)._
